spring:
  profiles:
    active: testmysql
---
spring:
  config:
    activate:
      on-profile: testmysql
  datasource:
    url: jdbc:mariadb://localhost:39907/codingtest
    username: root
    password: 1111

    hikari:
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
